{% include '_header.html.twig' %}

<h1>La liste des stagiaires</h1>
<table class="table">
    <thead>
        <tr>
        <th scope="col">#</th>
        <th scope="col">Nom</th>
        </tr>
    </thead>
    <tbody>
        {# {{ dump(stagiaires) }} #}

        {% for stagiaire in stagiaires %}
            <tr>
                <th scope="row">
                    {{ stagiaire.identifiant }}
                </th>
                <td>
                    {{ stagiaire.nom }}
                </td>
            </tr>
        {% endfor %}
        
  </tbody>
</table>

{% include '_footer.html.twig' %}